{
  "name": "moist",
  "version": "1.0.0",
  "description": "Moist — soil moisture monitor PWA wrapped as Android app",
  "private": true,
  "scripts": {
    "cap:sync": "cap sync",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "sharp": "^0.35.0",
    "web-push": "^3.6.7"
  }
}
